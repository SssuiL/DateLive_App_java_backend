# Java 后端工作边界

用户已明确要求 Java 后端独立管理，不改动当前 Python/FastAPI 后端的进度。

- Java 项目根目录为 `E:/Project/backend-java`。以后全部 Java 工作在此目录内完成，包括代码、依赖、构建、测试、数据库迁移、部署及进度文档。
- 旧 Python 项目位于 `E:/python_workspace/DateLive_App`，与 Java 工程独立。只读参考，不编辑、格式化、提交、重置或更新其进度。
- 提取旧契约时，不因导入旧应用而启动调度、创建文件、迁移数据库或写入旧环境；优先静态解析或使用隔离副本。
- 测试使用 Java 独立配置和隔离数据，不调用旧生产/测试环境的写接口，不加载旧 `.env` 或密钥。
- 不把旧依赖、上传文件、数据库快照和凭证批量复制进本项目。
- 客户端与旧后端已有改动属于独立工作，不在 Java 重构中顺手修改或覆盖。
- 项目尚未上线，无交期压力；目标是完整 Java 重建后一次验收切换，不需要长期双后端并行。
- 进度必须区分已盘点、已实现、已测试和已部署；不能把模拟供应商功能当成真实接入完成。

## GitHub 管理

- 用户指定 Java 后端远端为 https://github.com/SssuiL/DateLive_App_java_backend.git，后续 Java 迁移完成并验证后提交、推送到该仓库。
- 提交前检查暂存范围；.tools、.data、target、真实凭证、上传文件和本地数据库不提交。保留源码、迁移、测试、工具脚本及进度文档。
- 不强制推送、不覆盖远端已有提交；优先在 codex/ 前缀分支推进，报告提交和验证结果。
