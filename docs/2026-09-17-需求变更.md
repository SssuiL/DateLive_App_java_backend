# 2026-09-17 用户确认的 Java 后端需求变更

- 划卡取消会员和次数门槛，普通用户可无限普通喜欢；保留登录、拉黑、账号状态及审核等权限校验。
- 取消超级喜欢，不再创建超级喜欢会话、到期时间、额度或付费权益；只有双方普通喜欢后才配对。
- 群组支持生成可分享的 HTTPS 链接。域名通过 JAVA_GROUP_PUBLIC_BASE_URL 配置。用户指定测试域名为 https://sssuuil-server.top，已存入本地启动配置；DNS、TLS、反向代理和外部平台真实可用性须分别验证，不自动改动现有公网服务。
- 群成员可获取链接，访客可查看最小邀请信息，登录后按原入群规则申请。分享链接不绕过密码、审核、人数或仅邀请限制。
- 后端返回 share_url/share_title/share_text 和公开邀请页面；客户端的分享面板、平台 SDK、应用链接唤起及真实平台测试另行联调，尚不能标记完成。Python 与 Flutter 工程保持只读。
- 测试必须覆盖普通账号超过旧额度、单向喜欢不配对、双向喜欢并发幂等、超级喜欢不可用，以及链接解析、权限、失效、HTML 转义和入群规则。